1. In what way are React components meant to be "pure functions"

the pure fuctions are just taking only the parameters maybe i dont know correctly


2. What is a "side effect" in React? What are some examples?
the fetching of an data from an api which will leads to infinite loops


3. What is NOT a "side effect" in React? Examples?

just rendering the function that returns exact same value man

4. When does React run your useEffect function? When does it NOT run
   the effect function?
   
use effect run when we defined a variable or usestate in inside of useeffect which triggers this useeffect, it doesnt run after finishing its condition

5. How would you explain what the "dependecies array" is?

the empty array [] inside the useeffect that tells how many thimes does this has runned maybe 